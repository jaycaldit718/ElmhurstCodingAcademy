$(".hamburger").click(function () {

    $(this).toggleClass("is-active");
    $(".mobile-menu").toggle();

    //    This function toggles the active class on the hamburger class and toggles whether the mobile menu is hidden or displayed when the hamburger is clicked


});
